<!DOCTYPE html>
<html>
  <head>
    <title>EmployeeManagementApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar navbar-light" style="background-color: #81b1ce;">
  <h2 style="color: white;">Employee Management System</h2>
  <% if employee_signed_in? %>
  <div>
  <%= link_to 'Status', statuses_path, class: 'btn btn-outl  ine-primary' %>

  <%= link_to 'Leave', leaves_path, class: 'btn btn-outl  ine-primary' %>

      <%= link_to 'My Profile', profiles_show_path, class: 'btn btn-outl  ine-primary' %>
    <%= link_to 'Sign out', destroy_employee_session_path, class: 'btn btn-outl  ine-primary' %>
   </div>
  <% else %>
    <%= link_to 'Sign in', new_employee_session_path,  class: 'btn btn-outl  ine-primary'  %>
  <% end %>    
</nav>

  <p class="alert"><%= alert %></p>
  <%= yield %>
  </body>
</html>
